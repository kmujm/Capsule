<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/relativeLayout2"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginBottom="11dp">

    <ImageView
        android:id="@+id/image_adapter"
        android:layout_width="132dp"
        android:layout_height="132dp"
        android:clipToOutline="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/check_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_check"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/image_adapter"
        app:layout_constraintEnd_toEndOf="@id/image_adapter"
        app:layout_constraintStart_toStartOf="@id/image_adapter"
        app:layout_constraintTop_toTopOf="@id/image_adapter" />


</androidx.constraintlayout.widget.ConstraintLayout>